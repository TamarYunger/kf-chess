# Server Design — Scaling KungFu Chess

תיעוד זה עונה על שאלות ה-scale שקיבלנו בשבוע האחרון של ה-CTD, מתוך המצב
הקיים בפועל של השרת בריפו הזה (`server/ws_server.py`, `server/room.py`,
`server/matchmaking.py`, `server/db.py`, `server/protocol.py`). הרעיון הוא
לא לענות תיאורטית, אלא להצביע בדיוק מה בקוד הקיים ישבר תחת עומס, ולמה.

## המצב הקיים, בקצרה

היום הכל רץ כתהליך `asyncio` בודד (`ws_server.py`):

- **חיבורי WebSocket** מוחזקים בזיכרון של אותו תהליך.
- **`Room`** (המשחק החי, כולל `GameEngine` וה-tick loop שלו) חי בזיכרון
  של אותו תהליך, ולא בשום מקום אחר.
- **Matchmaking** (`server/matchmaking.py`) הוא `dict` פשוט בזיכרון של
  אותו תהליך — אין שום דבר חיצוני (DB/cache) שמתאם בין תהליכים.
- **חשבונות משתמשים** יושבים ב-SQLite (`server/accounts.db`, דרך
  `server/db.py`) — קובץ יחיד על הדיסק המקומי של אותו תהליך.
- **קצב השידור**: `TICK_INTERVAL_SECONDS = 0.05` (20Hz) — כל tick, לכל
  חיבור בחדר (שחקנים וצופים כאחד), נשלח JSON מלא של `encode_snapshot`
  (כל התאים, כל ה-moves/jumps בתנועה, **וכל היסטוריית המהלכים של המשחק
  עד כה**), לא רק מה שהשתנה.

זו ארכיטקטורה נכונה ומצוינת ל"שרת אחד, כמה חדרים" — אבל כל אחת מארבע
ההנחות האלה (חיבורים בזיכרון, חדרים בזיכרון, matchmaking בזיכרון, SQLite
מקומי) היא בדיוק הדבר שצריך להחליף כדי לתמוך במיליוני משתמשים.

---

## 1. DB ל-100 מיליון משתמשים רשומים — האם SQLite מתאים?

**לא.** לא בגלל נפח הנתונים (`users` זו טבלה דקה — username, hash, salt,
rating; 100 מיליון שורות זה בסדר גודל של כמה עשרות GB, שום DB מודרני לא
מתקשה עם זה), אלא בגלל **מודל הגישה**:

- SQLite הוא קובץ על דיסק, לא שירות רשת. `AccountStore.__init__` פותח
  `sqlite3.connect(path)` — מחייב שכל מי שנוגע ב-DB ירוץ על אותה מכונה
  ויגש לאותו קובץ. אבל אנחנו הולכים לפזר את שרת המשחק על פני הרבה
  קונטיינרים (ר' סעיף 2) — כולם צריכים לגשת לאותו מאגר משתמשים, מכל
  מקום ברשת. קובץ מקומי לא עונה על זה.
- SQLite נועל את כל הקובץ לכתיבה (writer יחיד בכל רגע נתון). עם מספיק
  שרתי login/register מקבילים זה הופך לצוואר בקבוק ולנקודת תקלה יחידה.
- אין replication מובנה, אין HA, אין partitioning — אם הדיסק הזה נופל,
  **כל** בסיס המשתמשים נופל.

**מה כן:** להפריד לפי דפוס הגישה, לא לשמור הכל באותו מקום:

- **חשבונות/אימות/דירוג (`AccountStore`)** — DB יחסי רגיל עם server
  אמיתי (PostgreSQL, למשל), לא embedded. השאילתות כאן (`SELECT ... WHERE
  username = ?`, `UPDATE rating`) הן בדיוק מה ש-DB יחסי עם אינדקס על
  `username` (שהוא כבר `PRIMARY KEY`) עושה טוב. עם primary אחד + כמה
  read replicas זה כנראה מספיק גם ב-100 מיליון רשומים, כי הנפח קטן וקצב
  ה-writes (login/register/עדכון rating) נמוך יחסית לקצב המהלכים
  במשחק עצמו. אם בעתיד ה-writes יהפכו לצוואר בקבוק — sharding לפי hash
  של `username` על כמה primaries (למשל דרך Citus/Vitess, או DB מבוזר
  כמו CockroachDB/Spanner אם צריך global consistency בין אזורים).
- **Session/token אחרי login, ותוצאת matchmaking** — לא ב-DB יחסי בכלל.
  אלה נתונים חמים, קצרי-טווח, בקצב גישה גבוה — שייכים ל-**cache
  בזיכרון-משותף** (Redis), לא לדיסק. זה גם בדיוק הרכיב שפותר את סעיף 2.
- PBKDF2 עם 100,000 iterations (`server/db.py`'s `PBKDF2_ITERATIONS`)
  זול מספיק היום כשזה תהליך בודד; בקנה מידה של אימותים במקביל שווה
  לשקול להעביר ל-worker/queue נפרד כדי שלא יחסום את אותו process שמנהל
  חיבורי WebSocket (זה לא שאלת ה-DB, אבל אותה סיבה: אל תשמרי הכל בתהליך
  אחד).

---

## 2. 10 מיליון משתמשים בו-זמנית — שרת אחד לא מספיק, איך מחלקים?

תהליך `asyncio` בודד לא יכול להחזיק 10 מיליון חיבורי WebSocket פתוחים —
מגבלת file descriptors של מערכת ההפעלה, ובעיקר: כל ה-CPU של ליבה אחת
צריך לקודד/לפענח JSON ולהריץ tick ל-20Hz על כל חדר. צריך הרבה מופעים
(הרבה קונטיינרים/Docker-ים), כל אחד מחזיק תת-קבוצה של החיבורים.

השאלה האמיתית היא: **מי מחזיק מה, ואיך שחקן מוצא את החדר שלו.**

### חלוקת תפקידים בין קונטיינרים

- **Gateway / Lobby (stateless, הרבה עותקים זהים)** — מטפל ב-`LOGIN`
  (מול DB/cache), ב-`PLAY` (matchmaking), וב-`ROOM CREATE`/`ROOM JOIN
  <id>`. אין לו זיכרון-מצב פרטי; אפשר להרוג ולהרים עותק בכל רגע.
- **Game Server (stateful)** — כל מופע מחזיק בזיכרון שלו כמה `Room`
  חיות (בדיוק כמו היום), עם ה-tick loop וה-`GameEngine`. **חדר חי
  שלם על מכונה אחת** — אין דרך פשוטה לפצל tick loop יחיד בין מכונות,
  אז זו יחידת ה-sharding הטבעית: room_id → game-server instance אחד.

### איך יודעים מי נמצא איפה

כאן `server/matchmaking.py`'s `dict` בזיכרון-תהליך לא שורד — הוא צריך
להפוך ל**רישום משותף חיצוני** (Redis, למשל):

- `room_id -> instance_id` — כשה-Gateway מקבל `ROOM JOIN <id>`, הוא בודק
  ברישום איזה game-server מחזיק את החדר הזה, ומפנה את הלקוח לשם (redirect
  ברמת ה-WebSocket handshake, או ה-gateway עצמו מגשר/proxy).
- **תור matchmaking גלובלי** (לא per-instance!) — כדי ש"כולם יכולים
  לשחק עם כולם" בפועל (לא רק מי שנחת באותו gateway), מאגר הממתינים
  (`find_opponent`'s `waiting`) צריך לחיות במקום משותף — למשל sorted-set
  ב-Redis לפי rating — כדי שכל gateway יוכל לדחוף/למשוך ממנו. ברגע
  שנמצא זיווג, ה-gateway מקצה את הזוג לגיים-סרבר עם מקום פנוי (שירות
  allocation/capacity-tracking, לא שונה בעיקרון מ-Agones/GameLift
  ב-multiplayer games אחרים).
- **Reconnect** (`opponent_disconnected`/grace period הקיים כבר בקוד)
  צריך **sticky routing**: שחקן שמתנתק וחוזר חייב לחזור לאותו game-server
  שמחזיק את החדר שלו, לא לאיזשהו עותק אחר — בדיוק בגלל אותו רישום
  `room_id -> instance_id`.

### מתח שכדאי לציין: גלובלי מול latency

"כל אחד יכול לשחק עם כל אחד, מכל העולם" מתנגש עם זה שזה משחק real-time
(`MOVE_DURATION`/`JUMP_DURATION` בסדר גודל שנייה) — latency חוצה-יבשות
(100–300ms) הוא כבר חלק משמעותי מתקציב התזמון הזה. בפועל: matchmaking
צריך להעדיף שכנות גיאוגרפית (אזור/region) קודם, וליפול חזרה ל-global רק
אם אין match מקומי — כלומר לא cluster אחד גלובלי אלא כמה clusters
אזוריים (בדיוק התפקיד של K8s/K3s כאן: לתאם fleet של game-server pods
בכל אזור, ולתת ל-gateway לדעת איזה region הכי קרוב ללקוח).

---

## 3. תעבורת רשת — כמה זה בפועל, והפער בין ה"הנחה" לקוד האמיתי

ה"הנחה" בשאלה היא צעד ממוצע כל 2 שניות. אבל שווה לבדוק את שני הכיוונים
בנפרד, כי הם שונים בסדרי גודל:

**כיוון לקוח → שרת** (פקודת טקסט, כמו `"MOVE e2 e4"`, כמה עשרות בייטים):

| | ערך |
|---|---|
| הודעות/שנייה (10M פעילים, פקודה כל 2 שניות) | ~5,000,000 msg/s |
| גודל הודעה | ~20–30 bytes |
| Bandwidth | ~100–150 MB/s בסה"כ |

זה **הרבה הודעות בשנייה** (דורש שכבת ingest מבוזרת אמיתית, לא סוקט בודד),
אבל במונחי בייטים זה בסדר גודל סביר לשירות גלובלי גדול — לא "כמות מידע"
עצומה.

**כיוון שרת → לקוח — וכאן הקוד הקיים חושף את הבעיה האמיתית:**
`ws_server.py`'s tick loop רץ ב-20Hz (`TICK_INTERVAL_SECONDS = 0.05`)
ומשדר **snapshot JSON מלא** (`encode_snapshot`) לכל חיבור בחדר, בכל
tick, **בלי קשר** לזה שמשתמש זז פעם כל 2 שניות. וה-snapshot כולל את כל
`move_history` שהצטבר עד עכשיו — כלומר ההודעה גדלה ככל שהמשחק מתקדם.

בהנחת גודל הודעה סביר (~1–3KB, תלוי כמה מהלכים כבר נצברו):

| | ערך |
|---|---|
| חיבורים מחוברים בו-זמנית | 10,000,000 |
| קצב שידור לכל חיבור | 20/שנייה |
| הודעות/שנייה בסה"כ | 200,000,000 msg/s |
| Bandwidth (בגודל הודעה ~1.5KB) | **~300 GB/s** |

זה בלתי אפשרי לחלוטין בקנה מידה הזה. המסקנה החשובה: **מה שקובע את
התעבורה זה לא קצב הקלט של המשתמש, אלא קצב ה-broadcast של השרת**.

### הפתרון: broadcast לפי אירוע, לא לפי טיימר

הרעיון הוא לא "לשלוח פחות snapshot-ים", אלא לוותר על snapshot תקופתי
כמעט לגמרי, ולשדר רק כשקרה משהו אמיתי — בדיוק כמו שהפרוטוקול כבר עושה
חלקית עם `arrival`/`game_over`, רק שהיום זה קורה **בנוסף** ל-snapshot
המלא, לא **במקום** אותו. זה עובד כאן דווקא כי לתנועה יש משך זמן קבוע
וידוע מראש (`MOVE_DURATION`/`JUMP_DURATION`) — הלקוח כבר יודע לצייר
אנימציה בעצמו בין "המהלך התחיל" ל-"המהלך נחת" (`view/animation.py`),
כך שהוא לא צריך עדכון מיקום כל 50ms כדי להיראות חלק.

בפועל, שלושה מקומות בקוד היו צריכים להשתנות כדי לממש את זה:

1. **`server/ws_server.py`** — `TICK_INTERVAL_SECONDS = 0.05` היום קובע
   גם את קצב הסימולציה הפנימית וגם את קצב מה שנשלח ברשת, באותה נשימה.
   צריך להפריד אותם: הסימולציה יכולה להישאר ב-20Hz, אבל תדירות השליחה
   ברשת צריכה להיגזר מאירועים, לא מהטיימר.
2. **`server/room.py`** — `_pending_events`/ה-flush שכבר קיים הוא בדיוק
   המנגנון שדוחף `arrival` ברגע שהוא קורה; הצעד החסר הוא להפסיק לשדר
   **גם** snapshot מלא בכל `tick()`/`handle_command()`, ולהשאיר את
   ה-snapshot המלא רק לרגע ההצטרפות/reconnect (keyframe חד-פעמי).
3. **`server/protocol.py`** — `encode_snapshot` לא נעלם, אבל נקרא הרבה
   יותר לעיתים רחוקות (keyframe בלבד); וכשכן נקרא, לא צריך לכלול את כל
   `move_history` שנצבר עד כה — הלקוח כבר קיבל כל מהלך פעם אחת דרך
   `arrival`, ויכול לצבור היסטוריה בעצמו במקום לקבל אותה שוב בכל פעם.

זה שינוי אמיתי ב-wire protocol, לא רק "לשלוח פחות בייטים" — ולכן הוא
מופיע כאן כמסקנת design, לא כמשהו שמומש בפועל בשבוע הזה.

---

## 4. משחק שנמשך 30–90 שניות — מה זה אומר על תפקידי הקונטיינרים

זו בדיוק הסיבה לחלק stateless/stateful מלמעלה, ומחדדת אותה:

- **Game-server containers הם short-lived מטבעם** — לא "קונטיינר אחד
  למשחק" (בזבזני, overhead של scheduling על כל 60 שניות משחק), אלא כל
  מופע מחזיק **הרבה חדרים קצרים במקביל**, וה-churn (חדרים שנפתחים
  ונסגרים) הוא מהיר מאוד — בסדר גודל של דקה, לא שעות.
- **Autoscaling וגם rolling deploy יכולים להיות פשוטים בזכות זה**: אין
  צורך ב-live-migration של מצב משחק בין מכונות. כדי להוריד מופע גיים-
  סרבר (deploy/scale-down) מספיק לסמן אותו "לא מקבל חדרים חדשים" ולחכות
  ~90 שניות עד שכל החדרים הקיימים שלו נגמרים לבד (graceful drain) — בדיוק
  ה-lifecycle שק8s/K3s עם readiness probes נותן "בחינם" (דומה למודל
  Ready → Allocated → Shutdown ב-Agones).
- **קיבולת (capacity) של game-server** נמדד ב"חדרים פעילים כרגע", ומתחדש
  כמעט כל דקה — אז אותות ה-autoscaling (לא CPU ממוצע לאורך שעה, אלא
  "כמה חדרים פנויים יש עכשיו בכל מופע") צריכים להגיב באותו טווח זמן קצר.
- לעומת זאת ה-**Gateway/matchmaking** לא קשור בכלל לאורך המשחק — הוא
  long-lived, stateless, ונפתח/נסגר לפי עומס בקשות (login/matchmaking),
  לא לפי אורך משחק.

---

## מה עוד נשאר ללמוד (המטלה המקורית)

- קורס ה-Docker — עוד לא הושלם, יש להשלים אותו בנפרד מהמסמך הזה.
- מחקר עצמאי על Kubernetes ו-K3s — הנקודות למעלה (fleet של game-server
  pods, readiness/drain לפי אורך משחק, autoscaling לפי חדרים פעילים,
  clusters אזוריים) הן בדיוק המקום שבו K8s/K3s נכנסים לתמונה; שווה
  לקרוא עליהם תוך התייחסות ישירה לתפקידים שתוארו כאן.
