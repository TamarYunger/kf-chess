# Server Design — Scaling KungFu Chess

## מטרת המסמך

תכנון ארכיטקטורת שרת שתומכת ב-100 מיליון משתמשים רשומים ו-10 מיליון
משתמשים בו-זמנית, בהתבסס על הארכיטקטורה הקיימת (שרת יחיד, `server/`
בריפו זה): חיבורי WebSocket וחדרי משחק בזיכרון תהליך יחיד, חשבונות
ב-SQLite, ושידור מצב בקצב קבוע לכל חיבור. המסמך מתאר את היעד — פירוק
אותה ארכיטקטורה ל-6 רכיבי שירות נפרדים (ר' הסעיף הבא), מתואמים דרך
NATS ו-Redis, לפי המימוש המדורג שסוכם.

---

## הנחות עבודה

הנחות הן חלק נורמלי והכרחי מתכנון — אי אפשר לחכות לסט דרישות מלא וחד-משמעי.
הכלל הוא לא להימנע מהנחות, אלא לשים כל אחת על השולחן, בכתב, כדי שקורא יוכל
לאתר אותה ולהתווכח איתה. הנחה לא-מתועדת היא מוקש; הנחה מתועדת היא רק
החלטת עיצוב שמישהו אחר רשאי לתקן.

| הנחה | בסיס (למה זו הנחה מושכלת) | מה נשבר אם טעות | איך נגלה |
|---|---|---|---|
| אורך משחק ממוצע ~60 שניות | אריתמטיקה על הטווח הנתון 30-90 שניות (אמצע הטווח) | חישוב ה-churn (סעיף "טופולוגיית שירותים": ~83,000 חדרים/שנייה) ולפיו קיבולת/autoscaling של Game Server Shard | למדוד את אורך המשחק בפועל בשבוע הראשון בפרודקשן |
| Latency חוצה-יבשות (100-300ms) מהווה חלק משמעותי מתקציב הזמן של מהלך | פיזיקת רשת ידועה בין-יבשתית, מול קצב מהלכים בסדר גודל של שנייה | ההחלטה על אשכולות (clusters) אזוריים במקום אשכול גלובלי יחיד, והעדפת שכנות גיאוגרפית ב-matchmaking | Playtest עם לקוחות אמיתיים ממיקומים גיאוגרפיים שונים, ואז מדידה מתמשכת |
| Power-of-two-choices "כמעט שקול" ל"הכי פנוי" לבחירת Shard | תוצאה ידועה בספרות load-balancing (לא נמדד ספציפית עבור המערכת הזו) | איזון העומס בפועל בין Game Server Shards; אם רחוק מ"הכי פנוי" בפועל, ייתכן צורך במנגנון בחירה מדויק יותר מול Redis | להשוות בפועל, תחת עומס, מול lookup מדויק של "מי הכי פנוי" |
| PostgreSQL primary אחד + read replicas מספיק בשלב ראשון (בלי sharding) | קצב הכתיבות (חשבונות/דירוג) נמוך משמעותית מקצב הקריאות, וללא צפי לגידול חד בקצב הכתיבה בטווח הקרוב | אם קצב הכתיבות יגדל משמעותית (למשל דרך תכונה עתידית), ה-primary היחיד יהפוך לצוואר בקבוק | לנטר את קצב הכתיבות בפועל מול הקיבולת של ה-primary |
| פקודת מהלך היא כ-25 בייט | מדידה על פורמט הטקסט הקיים ("MOVE e2 e4" וכיו"ב) | חישוב תעבורת הקלט המצרפית (סעיף "פרוטוקול תקשורת ונפח תעבורה") | למדוד בפועל את גודל ההודעות הממוצע בתעבורה אמיתית |

הכיוון של הסיכון חשוב לא פחות מהמספר עצמו: אם המשחק הממוצע נמשך בפועל 90
שניות ולא 60, ה-churn נמוך בכשליש מהמחושב — כלומר over-provisioned, לא
under. כיוון כשל כזה ("בנינו יותר headroom משצריך") בטוח משמעותית מהכיוון
ההפוך, וזה שווה לציון מפורש ליד כל הנחה.

---

## רכיבי השירות

פירוק לפי אחריות ודפוס עומס (מרחיב את הטבלה הישנה ב"טופולוגיית שירותים
וניתוב עומס" לשמות הרכיבים בפועל):

| רכיב | אופי | אחריות |
|---|---|---|
| **API Gateway** | Stateless, REST/HTTP | LOGIN (מנפיק token), וכל מה שאינו זמן-אמת: rooms, history |
| **WS Gateway** | Stateless, מחזיק חיבורים חיים | מאמת token מול Redis, שולח/מקבל הודעות זמן-אמת, מנתב פקודות ל-Game Server Shard הנכון |
| **Matchmaker** | Stateless, תור גלובלי ב-Redis | משדך שחקנים לפי דירוג (`server/matchmaking.py`'s `find_opponent`, ללא שינוי בלוגיקה עצמה) |
| **Game Allocator** | Stateless | מחליט על איזה Shard ייפתח room חדש (power-of-two-choices לפי דיווח קיבולת) |
| **Game Server Shards** | Stateful, כל room חי שלם על shard אחד | מריץ `GameEngine` — ה-source of truth היחיד לחוקי המשחק |
| **Observability** | Stateless | health checks, logs, metrics בסיסיים לכל שירות |

**LOGIN עובר ל-API Gateway ולא נשאר verb בתוך ה-WebSocket** — החלטה
מפורשת: הלקוח מבצע קריאת REST תחילה, מקבל token, ופותח את ה-WebSocket
איתו. זה משנה גם את `client/session/network_client.py`, לא רק את השרת.

**חלוקת התפקידים בין NATS ל-Redis** (משלימה, לא מתחרה):
- **NATS** הוא ה-event bus הפנימי בין השירותים — ניתוב פקודות (WS
  Gateway → Shard הנכון), פרסום עדכוני מצב חזרה (Shard → WS Gateway),
  ותיאום matchmaking/allocation. זהו "who talks to whom, right now".
- **Redis** הוא מאגר המצב המשותף שהשירותים קוראים/כותבים אליו: מיפוי
  `room_id → instance_id`, תור matchmaking, sessions/tokens, ודיווח
  קיבולת (heartbeat) של כל Shard. זהו "מה המצב הנוכחי", לא ערוץ שידור.

### בעיית ה-connection registry: איך Shard "שולח הודעה" לחיבור שאין לו

ל-Game Server Shard יש רק `connection_id` (מזהה אטום) — לא את אובייקט
ה-WebSocket האמיתי, שחי אך ורק בזיכרון תהליך ה-WS Gateway שמחזיק אותו.
זו לא בעיה שאפשר "לדחות" — היא חלק בלתי-נפרד מהפיצול עצמו: ברגע ש-Room
וה-socket האמיתי חיים בשני תהליכים נפרדים, לוגיקת המשחק לא יכולה לקרוא
`connection.send(...)` ישירות יותר.

**הפתרון**: `server/nats_connection.py`'s `NatsConnectionProxy` — אובייקט
שנראה בדיוק כמו חיבור מבחינת `Room` (יש לו רק `send(message)`), אבל בפועל
מפרסם ל-NATS. `Room` עצמה (`server/room.py`) לא משתנה בכלל ולא יודעת
שהפיצול קרה.

**החלטת עיצוב קריטית — subject per instance, לא subject per connection**:
שתי גישות אפשריות לניתוב ההודעה חזרה ל-Gateway הנכון:
1. ערוץ NATS נפרד לכל `connection_id` — פשוט למימוש, סביר עד אלפי חיבורים.
2. ערוץ NATS **אחד לכל instance של WS Gateway** (`outbox.<instance_id>`),
   + registry קטן ב-Redis (`connection_id → instance_id`) שה-proxy בודק
   לפני הפרסום. ה-Gateway נרשם לערוץ אחד בלבד, לא משנה כמה חיבורים הוא
   מחזיק, ומנתב פנימה (מקומית, ב-dict) ל-connection_id הספציפי.

**נבחרה גישה 2**, למרות שהיא לא נדרשת בפועל בגרסה הקטנה הנוכחית (shard
אחד, מעט חיבורים) — כי זו **החלטה שאי אפשר לשדרג בשקט מאוחר יותר**: היא
חלק מהחוזה (wire contract) בין שני תהליכים נפרדים, אז מעבר בין הגישות
דורש שינוי מתואם בשני הצדדים (Shard ו-Gateway) בו-זמנית, לא שינוי פנימי
באחד מהם. זה שונה מהותית מהחלטות אחרות במסמך הזה (כמו מבנה תור
ה-matchmaking) שבהן דחיית המימוש לא עולה כלום — כאן יש מחיר אמיתי
לבחירה הלא-נכונה מראש, ובקנה המידה שהמסמך הזה מצהיר עליו (10 מיליון
חיבורים בו-זמנית), subject per connection פשוט לא עומד.

---

## אחסון נתונים

יש להפריד בין שני סוגי מידע שונים באופיים:

**נתונים קבועים (חשבונות, אימות, דירוג)** — מסד נתונים יחסי עם שרת
רשת אמיתי (PostgreSQL), לא קובץ מקומי. נפח הנתונים (כמה עשרות GB
ב-100 מיליון משתמשים) אינו מהווה אתגר; האתגר הוא נגישות בו-זמנית
מהרבה שרתים מבוזרים, נעילת כתיבה, וזמינות גבוהה — דברים שקובץ מקומי
אינו מספק. primary אחד עם כמה read replicas מספיק בשלב ראשון; sharding
או מעבר ל-DB מבוזר גלובלי נדרשים רק אם קצב הכתיבות יגדל משמעותית.

**נתונים זמניים, בקצב גישה גבוה (session, תור matchmaking, מיפוי
ניתוב)** — שכבת cache בזיכרון-משותף (Redis), נגישה לכל השרתים ברשת.
אלה נתונים שאיבודם אינו קטסטרופלי ושאין טעם לשלם עבורם את המחיר
(איטיות, נעילות) של אחסון קבוע.

---

## טופולוגיית שירותים וניתוב עומס

שרת יחיד אינו יכול להחזיק מיליוני חיבורים בו-זמנית (מגבלת מערכת הפעלה
על חיבורים פתוחים, ומגבלת כוח עיבוד להרצת לוגיקת משחק בקנה מידה כזה).
הפתרון הוא חלוקה לרכיבים לפי דפוס העומס שלהם — ר' טבלת "רכיבי השירות"
למעלה. הסעיף הזה מתמקד בשני הסוגים המרכזיים מבחינת ניתוב עומס: **WS
Gateway** (stateless, ריבוי עותקים זהים) מול **Game Server Shard**
(stateful, כל חדר חי שלם על מכונה אחת).

מול ריבוי העותקים הזהים של ה-WS Gateway יושב **Load Balancer** — מפזר
חיבורים נכנסים ביניהם, וזו הנקודה שמאפשרת להרים/להוריד עותקי Gateway
בלי ששחקן ירגיש בכך.

חדר משחק חי אינו ניתן לפיצול בין מכונות — לכן room_id אחד תמיד מנוהל
על ידי shard יחיד, וזו יחידת החלוקה הטבעית של המערכת.

כדי ש"כל שחקן יוכל לשחק עם כל שחקן, ולהצטרף לכל חדר" בפועל, נדרש
רכיב תיאום משותף וחיצוני לכל השרתים. זה מתחלק בין NATS (ניתוב
ההודעות עצמן) ל-Redis (המצב המשותף שהניתוב מתבסס עליו) — ר' "חלוקת
התפקידים בין NATS ל-Redis" למעלה:

- מיפוי `room_id → instance_id` ב-Redis, כדי שכל WS Gateway ידע לאיזה
  Game Server Shard לנתב פקודה (דרך NATS) לשחקן המצטרף לחדר קיים,
  כולל בהתחברות מחדש לאחר ניתוק (ניתוב "דביק" לאותו shard). מיפוי זה
  נוצר, לא רק נקרא — כשחדר **חדש** צריך להיפתח (ROOM CREATE, או זוג
  ששודך ב-PLAY), **Game Allocator** הוא זה שמחליט על איזה instance
  הוא ייפתח:
  - כל Game Server Shard מדווח ל-Redis, בקצב קבוע, כמה חדרים פנויים יש
    לו כרגע (מתוך קיבולת מקסימלית ידועה מראש — נגזרת מ-bandwidth ו-CPU,
    ר' הסעיף על תעבורה).
  - Game Allocator שואל את Redis "מי הכי פנוי כרגע" (או שתי אפשרויות
    אקראיות ובוחר את הפחות עמוסה — power-of-two-choices, פשוט ליישום
    וכמעט שקול ל"הכי פנוי" מבחינת תוצאה), מקצה את החדר אליו, וכותב את
    המיפוי `room_id → instance_id` ל-Redis.
  - זהו בפועל שלב allocation נפרד, לא רק lookup — דומה בתפקידו למה
    ש-Agones (מסגרת ל-multiplayer game servers מעל Kubernetes) מספק
    כ-GameServerAllocation מובנה, אם רוצים לא לבנות את זה מאפס (מחוץ
    לתחום המימוש המדורג הנוכחי).
  - חלופה (פחות גמישה, אך בלי צורך בשלב allocation מרכזי): hashing
    עקבי (consistent hashing) של `room_id` על פני רשימת ה-instances
    הפעילים — קובע מיקום באופן דטרמיניסטי, אך מקשה על איזון עומס
    אחיד כש-instances מצטרפים/יוצאים.
- תור matchmaking גלובלי אחד ב-Redis (לא נפרד לכל WS Gateway), ממוין
  לפי דירוג, שה-**Matchmaker** קורא ממנו — אחרת שחקנים ישודכו רק בתוך
  אותו Gateway שקיבל את בקשתם.
- **בעלות בלעדית על חדר**: אם Game Server Shard קורס עם חדרים חיים
  עליו, ומופע חלופי מוקם במקומו, יש לוודא שלא ייווצר מצב שבו שני
  instances "חושבים" שניהם שהם הבעלים של אותו חדר בו-זמנית
  (split-brain) — נדרש מנגנון lock/lease (למשל ב-Redis) שמבטיח בעלות
  יחידה בכל רגע נתון, לא רק מיפוי סטטי.

מכיוון שזהו משחק בזמן-אמת עם תזמון מהלכים בסדר גודל של שנייה, latency
חוצה-יבשות (100-300ms) מהווה חלק משמעותי מתקציב זה — matchmaking צריך
להעדיף שכנות גיאוגרפית, והמערכת כולה בנויה כמספר אשכולות (clusters)
אזוריים ולא כאשכול גלובלי יחיד.

תעבורת הלקוחות (WebSocket) חייבת לרוץ מוצפנת (`wss://`) — לא נקודה
נפרדת בעיצוב, אבל דרישת בסיס שקל לפספס.

קיבולת החיבורים שכל Game Server instance יכול להחזיק בפועל אינה
נקבעת רק על ידי CPU — היא תלויה ישירות בנפח השידור לכל חיבור, כמפורט
בסעיף הבא.

פעולות שאינן חלק מהזמן-אמת של המשחק (למשל עדכון דירוג Elo לאחר
`game_over`) לא צריכות לחסום את ה-Game Server בזמן שהן מתבצעות — queue
קל מפריד ביניהן לבין הנתיב הקריטי, כדי שכתיבה ל-DB לא תעכב שידור
לשחקנים אחרים באותו instance.

---

## פרוטוקול תקשורת ונפח תעבורה

יש להפריד בין תעבורת קלט (שחקן → שרת) לתעבורת שידור (שרת → שחקן) —
הן שונות בסדרי גודל.

**תעבורת קלט**: פקודת מהלך היא הודעת טקסט קצרה (~25 בייט). גם בקנה
מידה של מיליוני שחקנים פעילים, זהו נפח נתונים מתון (מאות מגה-בייט
בשנייה בסך הכל) — אינו מהווה אתגר בפני עצמו, אם כי דורש שכבת קליטה
מבוזרת שיודעת להתמודד עם קצב ההודעות.

**תעבורת שידור** היא הצוואר-בקבוק האמיתי: אם השרת משדר עדכון מצב מלא
לכל חיבור **בקצב קבוע**, ללא תלות בפעילות השחקן בפועל, נפח התעבורה
נקבע לחלוטין על ידי מספר החיבורים וקצב השידור — ולא על ידי קצב הקלט.
בהינתן מיליוני חיבורים בו-זמנית, שידור מלא עשרות פעמים בשנייה לכל אחד
מהם מגיע לסדרי גודל של מאות GB בשנייה — בלתי אפשרי בכל תשתית סבירה.

**מסקנת עיצוב**: השידור חייב להיות מונע-אירועים, לא מבוסס-טיימר —
עדכון נשלח רק כשמשהו משמעותי קורה בפועל (מהלך מתחיל, מהלך נוחת, המשחק
מסתיים), ולא על בסיס מרווח זמן קבוע. מצב מלא (snapshot) נשלח רק
כ"נקודת עדכון" חד-פעמית — בהצטרפות ראשונית לחדר או בהתחברות מחדש —
ואינו כולל מידע היסטורי שכבר נמסר בעבר. שינוי כזה מקטין את נפח
התעבורה בסדרי גודל, והופך את השרת מ"תלוי במספר חיבורים בלבד" ל"תלוי
בפעילות בפועל".

---

## מחזור חיים ותשתית קונטיינרים

לסוגי השירותים השונים יש דפוסי חיים שונים לחלוטין, וזה צריך לבוא לידי
ביטוי בתכנון הקונטיינרים:

- **API Gateway / WS Gateway** — long-lived, stateless, גודלם נקבע לפי
  עומס בקשות (login/matchmaking/חיבורים) בלבד; אינם קשורים בשום צורה
  לאורך חיי משחק בודד. אותו הגיון חל על **Matchmaker** ו-**Game
  Allocator** — גם הם stateless, גודלם נקבע לפי קצב בקשות ולא לפי
  חדרים.
- **Game Server Shard** — כל instance מחזיק בו-זמנית הרבה חדרי משחק
  קצרי-חיים (לא קונטיינר אחד למשחק — בזבזני וחסר תועלת בקנה מידה של
  מיליוני משחקים במקביל). לפי חוק Little (מספר ממוצע = קצב הגעה × זמן
  שהייה ממוצע במערכת): 10 מיליון שחקנים ÷ 2 = 5 מיליון משחקים בו-זמנית;
  חלקי ~60 שניות אורך ממוצע — כ-83,000 חדרים נפתחים ונסגרים **בכל
  שנייה**. קצב תחלופה כזה מייתר את הצורך במנגנוני live-migration
  מורכבים: הורדת
  instance מבוצעת פשוט על ידי חסימת קליטת חדרים חדשים והמתנה קצרה עד
  שהחדרים הקיימים מסתיימים מעצמם (graceful drain) — דפוס הנתמך ישירות
  על ידי readiness probes בסביבת אורקסטרציה (Kubernetes/K3s).
- מדדי קיבולת ו-autoscaling ל-Game Server Shard צריכים להתבסס על מספר
  חדרים פעילים כרגע (מתעדכן כמעט כל דקה), לא על ממוצע ארוך-טווח — כלומר
  נדרש איסוף מדדים (metrics) בזמן-אמת מכל instance (למשל Prometheus),
  לא רק לוגים; החלטת autoscaling נשענת ישירות על המדד הזה. זהו בדיוק
  התפקיד של רכיב ה-**Observability** בטבלת "רכיבי השירות".

תחלופה מהירה של חדרים מכפילה את עלות השידור לכל חדר חדש שנפתח — כך
שמודל "הרבה חדרים קצרים על instance אחד" תלוי במעבר לשידור מונע-אירועים
שתואר לעיל; ללא זה, כל חדר חדש חוזר ומשלם את אותה עלות רשת מההתחלה.

---

## סיכום

| נושא | מסקנה |
|---|---|
| אחסון | הפרדה בין DB יחסי (נתונים קבועים, PostgreSQL) ל-cache משותף (נתונים זמניים, Redis) — לא קובץ מקומי אחד לכל סוגי המידע |
| רכיבים | 6 שירותים נפרדים (API Gateway, WS Gateway, Matchmaker, Game Allocator, Game Server Shards, Observability) — stateless מול Game Server Shard ה-stateful, מתואמים דרך NATS (ניתוב הודעות) ו-Redis (מצב משותף) |
| אימות | LOGIN עובר ל-API Gateway (token), לא נשאר verb בתוך ה-WebSocket |
| תעבורה | קצב השידור של השרת, לא קצב הקלט של השחקן, קובע את העומס — נדרש שידור מונע-אירועים |
| תשתית | קונטיינרי משחק קצרי-חיים ורבי-חדרים, מנוהלים ב-graceful drain במקום live-migration |
